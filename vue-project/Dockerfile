# Используем официальный образ Node.js версии LTS в качестве основы
FROM node:lts-alpine

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файл package.json и package-lock.json для установки зависимостей
COPY package*.json ./

# Устанавливаем зависимости проекта
RUN npm install

# Копируем все файлы проекта в текущую директорию внутри контейнера
COPY . .

# Собираем приложение для production с минимизацией
RUN npm run build

# Открываем порт 80 для обслуживания приложения
EXPOSE 80

# Запускаем приложение при старте контейнера
CMD ["npm", "run", "serve"]